-- liquibase formatted sql

-- changeset system:baseline
-- comment: Baseline marker for existing database schema
-- Это пустой changeset, который отмечает, что схема уже существует
SELECT 1 WHERE 1 = 0;